<!DOCTYPE html>
<html>
  <head>
    <title>Admin Panel - <%= Rails.application.class.module_parent_name %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  </head>

  <body>
    <!-- Navigation Bar (consistent with main app) -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
      <div class="container">
        <%= link_to "RoR Auth App", root_path, class: "navbar-brand" %>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <%= link_to "🛡️ Admin Dashboard", admin_path, class: "nav-link #{'active fw-bold' if current_page?(admin_path)}" %>
            </li>
            <li class="nav-item">
              <%= link_to "👥 Users", admin_users_path, class: "nav-link #{'active fw-bold' if current_page?(admin_users_path)}" %>
            </li>
            <li class="nav-item">
              <%= link_to "📝 Posts", admin_posts_path, class: "nav-link #{'active fw-bold' if current_page?(admin_posts_path)}" %>
            </li>
            <li class="nav-item">
              <%= link_to "💬 Comments", admin_comments_path, class: "nav-link #{'active fw-bold' if current_page?(admin_comments_path)}" %>
            </li>
            <li class="nav-item">
              <%= link_to "📊 Analytics", admin_analytics_path, class: "nav-link #{'active fw-bold' if current_page?(admin_analytics_path)}" %>
            </li>
            <li class="nav-item">
              <%= link_to "ℹ️ System Info", admin_system_info_path, class: "nav-link #{'active fw-bold' if current_page?(admin_system_info_path)}" %>
            </li>
          </ul>
          <ul class="navbar-nav ms-auto">
            <li class="nav-item dropdown">
              <a class="nav-link dropdown-toggle" href="#" id="adminDropdown" role="button" data-bs-toggle="dropdown">
                <i class="bi bi-person-circle"></i> <%= current_user.display_name %>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Dashboard", dashboard_path, class: "dropdown-item" %></li>
                <li><%= link_to "Main Site", root_path, class: "dropdown-item" %></li>
                <li><hr class="dropdown-divider"></li>
                <li>
                  <%= form_with url: destroy_user_session_path, method: :delete, local: true, class: "d-inline" do %>
                    <button type="submit" class="dropdown-item btn btn-link p-0 border-0 w-100 text-start" style="background: none; color: inherit;">
                      Sign Out
                    </button>
                  <% end %>
                </li>
              </ul>
            </li>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Admin Header -->
    <div class="admin-header">
      <div class="container">
        <div class="row align-items-center">
          <div class="col-md-8">
            <h1 class="h3 mb-1">
              🛡️ Admin Panel
            </h1>
            <p class="welcome-message">
              Comprehensive system management
            </p>
          </div>
          <div class="col-md-4 text-md-end">
            <p class="mb-1 welcome-message">
              Welcome, <%= current_user.display_name %> 🔒
            </p>
            <%= link_to "Logout", destroy_user_session_path, 
                method: :delete, 
                class: "btn btn-logout btn-sm",
                data: { confirm: "Are you sure?" } %>
          </div>
        </div>
      </div>
    </div>

    <main class="container mt-4">
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show admin-alert" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>

      <% if alert %>
        <div class="alert alert-danger alert-dismissible fade show admin-alert" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>
            </h1>
          </div>
         
          </div>
        </div>
      </div>
    </div>

    <main class="container mt-4">
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show admin-alert" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>

      <% if alert %>
        <div class="alert alert-danger alert-dismissible fade show admin-alert" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
